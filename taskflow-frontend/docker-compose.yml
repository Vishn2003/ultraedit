version: '3.8'
services:
  mock-api:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "4000:4000"
    command: npx json-server --watch db.json --port 4000

  frontend:
    build: .
    ports:
      - "3000:3000"
    depends_on:
      - mock-api
    environment:
      - VITE_API_URL=http://localhost:4000
